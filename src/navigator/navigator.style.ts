import {StyleSheet} from 'react-native';
import colors from '../styles/colors';

export const styles = StyleSheet.create({
  tabBarLabel: {
    width: 20,
    height: 2,
    backgroundColor: colors.tabInTabBar,
    marginTop: 12,
    marginBottom: 4,
  },
});
